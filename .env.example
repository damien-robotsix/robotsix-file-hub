# robotsix-file-hub reads its configuration from a single JSON file —
# `config/config.json` by default — not from environment variables.
# Set ROBOTSIX_CONFIG_FILE to point at a different file if needed.
ROBOTSIX_CONFIG_FILE=config/config.json
